name: Prod Synk

on:
    workflow_dispatch:
        inputs:
            environment:
                description: 'prod'
                required: true
                default: 'prod'
                type: choice
                options:
                - prod

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4
            - name: Build the Docker image
              run: echo "running dev"
