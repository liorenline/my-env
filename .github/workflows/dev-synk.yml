name: Dev Synk

on:
    workflow_dispatch:
        inputs:
            environment:
                description: 'dev'
                required: true
                default: 'dev'
                type: choice
                options:
                - dev

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4
            - name: Build the Docker image
              run: echo "running"
