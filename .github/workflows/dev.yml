name: Dev Synk

on:
    workflow_dispatch:
        inputs:
            environment:
                description: 'dev'
                required: true
                default: 'dev'
                type: choice
                options:
                - dev

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: hashicorp/setup-terraform@v4
            - name: Terraform dev
              working-directory: .
              run: echo "running"
